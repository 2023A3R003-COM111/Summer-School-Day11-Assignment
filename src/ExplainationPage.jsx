import React from "react";

export function ExplanationPage() {
  return (
    <div>
      <h2 className="page-title">Explanation</h2>
      <p>
        The application fetches car data from an API and displays it in a grid layout. React Router handles navigation,
        and components are modularized for maintainability. Basic CSS styles the UI to keep it clean and responsive.
      </p>
    </div>
  );
}
